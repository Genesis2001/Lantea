connection
{
	server = "irc.cncfps.com"
	port = 6667

	nick = "Lantea"
	name = "Lantea Bot Framework"
}

modules
{
	directory = "modules"
}

log
{
	target = "lantea.log"

	threshold = 31
}

# todo: should make these include config\ in the value field...
include { name = "config\autojoin.conf"; }
include { name = "config\uptime.conf"; }
